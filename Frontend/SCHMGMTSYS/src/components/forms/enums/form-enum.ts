export enum FieldType {
    Input = 'input',
    Textarea = 'textarea',
    Select = 'select',
    Checkbox = 'checkbox',
    DatePicker = 'datePicker',
};

export enum FieldName {
    Parent = 'parent'
};